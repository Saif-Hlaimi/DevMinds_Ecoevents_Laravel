pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Checkout the code from Git
                checkout scm
                echo "Checked out code from ${env.GIT_URL}"
            }
        }
    }
}